<%= form_for(@question) do |f| %>
  <% if @question.errors.any? %>
    <div id="error_explanation">
      <p><%= pluralize(@question.errors.count, "error") %> prohibited this event from being saved:</p>

      <ul>
      <% @question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "Title:" %><br />
    <%= f.text_field :title, id: :question_title %>
  </div>

  <div class="field">
    <%= f.label :description, "Description:" %><br />
    <%= f.text_area :description, id: :question_description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
